# Model catalog. This file, not code, is the list of models the product can use.
# Add a model by adding a [[model]] block. Change a model by editing one. Nothing
# in here is a secret: API keys are read at runtime from the environment variable
# named in api_key_env, never stored here.
#
# Fields:
#   id            stable slug used everywhere (do not rename once shipped)
#   display_name  what the frontend shows
#   provider      stub | ollama | openai_compat | together | fireworks | deepinfra | groq
#   model         the provider's own model string
#   base_url      hosted endpoint (OpenAI-compatible providers)
#   api_key_env   environment variable holding the key (hosted only)
#   host          Ollama host (local only)
#   tier          local | hosted
#   enabled       whether it appears in the picker
#   notes         free text
#
# Note on Anthropic Claude and Google Gemini: their APIs are not OpenAI-compatible,
# so they need a dedicated provider kind (a future catalog + provider addition).
# The catalog is built to absorb new provider kinds without touching callers.

[[model]]
id = "stub-echo"
display_name = "Stub (echo, offline)"
provider = "stub"
model = ""
tier = "local"
enabled = true
notes = "Deterministic echo. Baseline for offline runs and tests. Changes nothing."

[[model]]
id = "qwen3-8b-ollama"
display_name = "Qwen3 8B (local Ollama)"
provider = "ollama"
model = "qwen3:8b"
host = "http://localhost:11434"
tier = "local"
enabled = true
notes = "Local dev. Disable thinking or strip <think> tags before judging output."

[[model]]
id = "qwen3-235b-together"
display_name = "Qwen3 235B A22B (Together)"
provider = "together"
model = "Qwen/Qwen3-235B-A22B-fp8-tput"
base_url = "https://api.together.xyz/v1"
api_key_env = "TOGETHER_API_KEY"
tier = "hosted"
enabled = false
notes = "Large Qwen3. Enable when TOGETHER_API_KEY is set."

[[model]]
id = "deepseek-v3-together"
display_name = "DeepSeek V3 (Together)"
provider = "together"
model = "deepseek-ai/DeepSeek-V3"
base_url = "https://api.together.xyz/v1"
api_key_env = "TOGETHER_API_KEY"
tier = "hosted"
enabled = false
notes = "Strong open-weights generalist."

[[model]]
id = "llama-3.3-70b-together"
display_name = "Llama 3.3 70B (Together)"
provider = "together"
model = "meta-llama/Llama-3.3-70B-Instruct-Turbo"
base_url = "https://api.together.xyz/v1"
api_key_env = "TOGETHER_API_KEY"
tier = "hosted"
enabled = false
notes = "Natural English prose. Check the Llama license against your scale."

[[model]]
id = "mistral-large-latest"
display_name = "Mistral Large (Mistral)"
provider = "openai_compat"
model = "mistral-large-latest"
base_url = "https://api.mistral.ai/v1"
api_key_env = "MISTRAL_API_KEY"
tier = "hosted"
enabled = false
notes = "Clean, direct prose. OpenAI-compatible endpoint."

[[model]]
id = "gpt-4o-openai"
display_name = "GPT-4o (OpenAI)"
provider = "openai_compat"
model = "gpt-4o"
base_url = "https://api.openai.com/v1"
api_key_env = "OPENAI_API_KEY"
tier = "hosted"
enabled = false
notes = "High naturalness ceiling. Per-token cost."

# --- OpenAI open-weight models (gpt-oss), Apache 2.0, via a fast host ---

[[model]]
id = "gpt-oss-120b-groq"
display_name = "GPT-OSS 120B (Groq)"
provider = "openai_compat"
model = "openai/gpt-oss-120b"
base_url = "https://api.groq.com/openai/v1"
api_key_env = "GROQ_API_KEY"
tier = "hosted"
enabled = false
notes = "OpenAI's open model, served fast by Groq. Also on Together and Ollama."

[[model]]
id = "gpt-oss-20b-ollama"
display_name = "GPT-OSS 20B (local Ollama)"
provider = "ollama"
model = "gpt-oss:20b"
host = "http://localhost:11434"
tier = "local"
enabled = true
notes = "OpenAI's small open model, run locally. Needs `ollama pull gpt-oss:20b`."

# --- Gemini via Google's OpenAI-compatible endpoint ---

[[model]]
id = "gemini-2.5-pro"
display_name = "Gemini 2.5 Pro (Google)"
provider = "openai_compat"
model = "gemini-2.5-pro"
base_url = "https://generativelanguage.googleapis.com/v1beta/openai/"
api_key_env = "GEMINI_API_KEY"
tier = "hosted"
enabled = false
notes = "Google's OpenAI-compatible layer. Verify the current model id."

# --- Hugging Face open models via the HF OpenAI-compatible router ---

[[model]]
id = "hf-llama-3.3-70b"
display_name = "Llama 3.3 70B (Hugging Face)"
provider = "openai_compat"
model = "meta-llama/Llama-3.3-70B-Instruct"
base_url = "https://router.huggingface.co/v1"
api_key_env = "HF_TOKEN"
tier = "hosted"
enabled = false
notes = "Any HF-hosted chat model. Swap the model id for others on HF."

# --- OpenRouter: one key reaches almost everything (OpenAI-compatible) ---
# Verify current model ids at openrouter.ai/models; they change over time.

[[model]]
id = "openrouter-claude"
display_name = "Claude (via OpenRouter)"
provider = "openai_compat"
model = "anthropic/claude-sonnet-4"
base_url = "https://openrouter.ai/api/v1"
api_key_env = "OPENROUTER_API_KEY"
tier = "hosted"
enabled = false
notes = "Reaches Claude without a dedicated Anthropic provider."

[[model]]
id = "openrouter-gemini"
display_name = "Gemini (via OpenRouter)"
provider = "openai_compat"
model = "google/gemini-2.5-pro"
base_url = "https://openrouter.ai/api/v1"
api_key_env = "OPENROUTER_API_KEY"
tier = "hosted"
enabled = false
notes = "Same key as the other OpenRouter entries."

[[model]]
id = "openrouter-gpt-oss-120b"
display_name = "GPT-OSS 120B (via OpenRouter)"
provider = "openai_compat"
model = "openai/gpt-oss-120b"
base_url = "https://openrouter.ai/api/v1"
api_key_env = "OPENROUTER_API_KEY"
tier = "hosted"
enabled = false
notes = "Open-weight model through OpenRouter."
