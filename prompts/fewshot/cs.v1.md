Field: computer science.

BEFORE:
The utilisation of the proposed method leads to an improvement of accuracy by
⟦PAA⟧ on the benchmark dataset ⟦PAB⟧, and it can be observed that the model
outperforms the baseline.

AFTER:
The proposed method improves accuracy by ⟦PAA⟧ on the ⟦PAB⟧ benchmark,
outperforming the baseline.
